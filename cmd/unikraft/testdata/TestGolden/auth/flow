$ unikraft login --check

stderr:
	│ existing authentication token found organization=test

$ unikraft profile list

stdout:
	NAME     ACTIVE  METROS
	default  true    test

$ unikraft metro list

stdout:
	NAME  COUNTRY  ENDPOINT
	test  xx       https://api.unikraft.internal/

$ unikraft logout

stderr:
	│ logout successful

$ unikraft profile list

stdout:
	NAME  ACTIVE  METROS

$ unikraft metro list

stderr:
	│  
	│ error:
	│   profile not setup;
	│   
	│   use `unikraft login` to get started,
	│   
	│   or visit https://unikraft.com/docs/cli for more information
	│

exit code: 1
